version: "3.9"

services:
  web:
    build: .
    container_name: sushi_api
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=development
      - DATABASE_URL=postgresql://postgres:postgres@db:5432/sushi_db
    depends_on:
      - db
    volumes:
      - .:/app

  db:
    image: postgres:15
    container_name: sushi_db
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: sushi_db
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
